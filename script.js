FUNCTION polindrome(refer : STRING):STRING

VAR
    refer = STRING;
    i = INTEGER;
BEGIN
    IF (sting.length <= 1) THEN 
    write ("true")
    END_IF
for (let index = 0; index < array.length; index++) {
    const element = array[index];
    
}